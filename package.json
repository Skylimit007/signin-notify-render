{
  "name": "login-notifications",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Building...' && echo 'No additional build steps needed'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jose": "^4.14.4",
    "nodemailer": "^6.9.3"
  },
  "engines": {
    "node": "18.x"
  }
}
